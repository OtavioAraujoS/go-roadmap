# 🦫 Roadmap de Go — Do Zero ao Intermediário

Bem-vindo ao roadmap definitivo para aprender **Go (Golang)** do zero até o nível intermediário.  
Este guia é organizado em passos claros e progressivos, para você aprender de forma prática e estruturada.

---

## 📌 1. Instalação e Primeiros Passos

### 🔧 Instale o Go
Baixe a versão mais recente:

➡️ https://go.dev/dl

### 📂 Seu primeiro projeto
Crie uma pasta chamada `hello-go` e pratique:

- `go run` — executa o código sem compilar
- `go build` — compila e gera um binário
- `go mod init` — inicia um módulo Go

**🎯 Objetivo:** aprender a rodar e compilar programas Go.

---

## 📌 2. Fundamentos da Linguagem

Conceitos essenciais para qualquer projeto:

- Variáveis e tipos (`string`, `int`, `bool`, `float`, `rune`)
- Funções e múltiplos retornos
- Condicionais (`if`, `switch`)
- Loops (`for`)
- Arrays
- Slices
- Maps
- Structs
- Ponteiros (importante: Go usa passagem por valor)

---

## 📌 3. Pacotes e Organização de Código

Aprenda a estruturar projetos como um profissional:

- Uso de `package` e `import`
- Separar código em arquivos e pastas
- Criar módulos com `go mod`
- Organização de diretórios para projetos maiores

**🎯 Objetivo:** manter código limpo e bem dividido.

---

## 📌 4. Manipulação de Arquivos e JSON

Domine operações comuns:

- Ler e escrever arquivos (`os`, `io`)
- Serializar e desserializar JSON (`encoding/json`)

Essencial para CLIs, APIs e microsserviços.

---

## 📌 5. Web Servers e Conexões

Aprenda a criar APIs com a biblioteca padrão:

- Criar servidor com `net/http`
- Definir rotas
- Criar handlers
- Criar middlewares simples

**🎯 Esse passo abre portas para APIs profissionais.**

---

## 📌 6. Concorrência em Go (O Poder da Linguagem)

Go brilha aqui:

- **Goroutines**
- **Channels**
- WaitGroups
- Mutex (básico)

**🎯 Dominar isso significa entender a alma da linguagem.**

---

## 📌 7. Testes

Go possui uma excelente infraestrutura nativa para testes:

- Criar testes com `go test`
- Testes unitários
- Testes de API
- Benchmarks (`go test -bench .`)

---

## 📌 8. Frameworks e Práticas Avançadas

Depois da base sólida, estude:

### 🌐 Web
- **Gin** (framework rápido e produtivo)

### 💾 Banco de dados
- **Gorm** (ORM SQL)

### 🔧 Utilidades
- Logging: `zerolog` ou `logrus`
- Env vars: `godotenv`

---

## 📌 9. Deploy e Produção

Aprenda a colocar seus apps no ar:

- Build otimizado para produção
- Binários estáticos
- Docker + Go
- Configuração por ambiente
- Documentação de APIs
